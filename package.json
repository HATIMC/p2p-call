{
  "name": "p2p-video-call",
  "version": "2.0.0",
  "description": "P2P WebRTC video calling — REST registry + PeerJS signaling",
  "main": "server.js",
  "scripts": {
    "build": "rm -rf dist && esbuild server.js --bundle --platform=node --target=node20 --format=cjs --outfile=dist/server.js --external:better-sqlite3 && cp -R public dist/public",
    "start": "node server.js",
    "start:render": "node dist/server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.0.0",
    "express": "^4.21.0",
    "selfsigned": "^5.5.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
